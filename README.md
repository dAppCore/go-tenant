# go-tenant

CoreGO tenant service primitives for workspace resolution, user context,
feature entitlements, usage tracking, and PHP tenant API integration.

The Go module lives in `go/`. Development uses root `go.work`; release and CI
verification use `GOWORK=off` inside `go/`.
