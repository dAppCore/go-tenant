# go-tenant Documentation

`go-tenant` provides CoreGO service components for multi-tenant applications.
It resolves workspaces, carries authenticated user context, checks feature
entitlements, records usage, and maintains local cache state.
